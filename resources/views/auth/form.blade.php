@extends('layouts.public')
@section('title', 'Admin sign in — Bheem Chand')
@section('content')<section class="section wrap"><div class="auth-card panel"><span class="eyebrow">PORTFOLIO STUDIO</span><h1>{{ ['login' => 'Welcome back.', 'forgot' => 'Reset your password.', 'reset' => 'Choose a new password.'][$mode] }}</h1>@include('partials.messages')<form method="post" action="{{ ['login' => '/admin/login', 'forgot' => '/admin/forgot-password', 'reset' => '/admin/reset-password'][$mode] }}">@csrf @if($mode === 'reset')<input type="hidden" name="token" value="{{ $token }}">@endif<label for="email">Email address</label><input id="email" name="email" type="email" value="{{ old('email', request('email')) }}" required autocomplete="username">@if($mode !== 'forgot')<label for="password">Password</label><input id="password" name="password" type="password" required autocomplete="{{ $mode === 'login' ? 'current-password' : 'new-password' }}" @if($mode === 'reset') minlength="12" @endif>@endif @if($mode === 'reset')<label for="password_confirmation">Confirm password</label><input id="password_confirmation" name="password_confirmation" type="password" required minlength="12" autocomplete="new-password">@endif<button class="button primary">{{ ['login' => 'Sign in →', 'forgot' => 'Send reset link →', 'reset' => 'Save password →'][$mode] }}</button></form><a class="text-link" href="{{ $mode === 'login' ? '/admin/forgot-password' : '/admin/login' }}">{{ $mode === 'login' ? 'Forgot password?' : 'Back to sign in' }}</a></div></section>@endsection
