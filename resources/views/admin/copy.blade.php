@extends('layouts.admin')
@section('content')<div class="eyebrow">WORDS ACROSS YOUR WEBSITE</div><h1>Page copy & chat prompts</h1><p>Manage section headings, introductions, the homepage stack, and assistant starter questions. Profile facts are edited in Profile & settings.</p><form class="admin-form panel" method="post" action="/admin/copy">@csrf @foreach($copy as $key => $value)<label for="{{ $key }}">{{ ucfirst(str_replace('_', ' ', $key)) }}</label><textarea id="{{ $key }}" name="{{ $key }}" rows="{{ $key === 'chat_starters' ? 4 : 2 }}" required maxlength="1500">{{ old($key, $value) }}</textarea>@if($key === 'stack')<small>Separate technologies with commas.</small>@elseif($key === 'chat_starters')<small>One question per line. The first five are displayed.</small>@endif @endforeach<button class="button primary">Save page copy ↗</button></form>@endsection
